Give me one direct downloadable URL for a world-country GeoJSON file suitable for this POC.

Requirements:

country polygons
ISO2 or ISO3 country identifiers
no API
no runtime dependency
file can be downloaded once and bundled locally
compatible with the internal-use guidance you just provided

I specifically want GeoJSON, not TopoJSON, because I do not currently have D3 or topojson-client installed.

Do not give implementation instructions. Just give me the recommended source URL and exact file name.
