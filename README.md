The supplied local world-country SVG is now available at:

frontend/public/world-country.svg

Use this asset only.

Do not install D3.
Do not make external map/tile/API calls.
Render it directly with React/SVG and continue the previously approved
country matching, hover, selection, filtering, tooltip, and reconciliation work.
