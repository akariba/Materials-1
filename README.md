I need the actual map geometry asset, not implementation guidance.

Do you have access to an approved/internal world-country boundary file that I can download for a React/Vite POC?

Accepted formats:

SVG with separate identifiable country paths, preferably ISO2/ISO3 IDs
GeoJSON
TopoJSON

Requirements:

complete world-country boundaries
suitable for country-level choropleth/interactivity
no external runtime calls
no external map tiles
no API key
must be permitted for internal Citi use

If such an asset exists, attach the actual file as a downloadable artifact and state its source.

Do NOT generate approximate/fabricated country geometry.
Do NOT merely give me a URL or sample code.
If no approved asset is accessible to you, say so explicitly.
